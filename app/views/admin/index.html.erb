<% @users.each do |user| %>
	<div>Name: <%= user.name %> Email: <%= user.email%></div>
	<%= link_to 'Delete', user_path(user), class: "btn btn-primary btn-edit", :confirm => 'Are you sure you want to delete this user?', :method => :delete %>
<% end %>