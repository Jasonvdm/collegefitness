<%= simple_form_for(@workout) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name %>
    <%= f.input :description %>
    <%= f.input :difficulty %>
    <%= f.input :equipment %>
    <%= f.input :equipmentneeded %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
